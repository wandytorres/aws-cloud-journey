apiVersion: v1
kind: Service
metadata:
  name: nginx-demo
  namespace: ansible-demo
spec:
  selector:
    app: nginx-demo
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
